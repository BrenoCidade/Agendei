{
  "name": "agendei",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:all": "concurrently -n server,web -c cyan,magenta \"pnpm --filter server dev\" \"pnpm --filter @saas/web dev\"",
    "dev": "pnpm dev:all",
    "start:dev": "pnpm -r --parallel run start:dev",
    "e2e:backend": "pnpm --filter server run test:e2e:full",
    "e2e:web:install": "pnpm --filter @saas/web run test:e2e:install",
    "e2e:web": "pnpm --filter @saas/web run test:e2e",
    "e2e:web:auth": "pnpm --filter @saas/web run test:e2e:auth",
    "e2e:web:services": "pnpm --filter @saas/web run test:e2e:services",
    "e2e:web:appointments": "pnpm --filter @saas/web run test:e2e:appointments",
    "e2e:backend:regression": "pnpm --filter server run test:e2e:regression",
    "e2e:all": "pnpm run e2e:backend && pnpm run e2e:web",
    "lint": "pnpm -r --if-present lint",
    "type-check": "pnpm -r --if-present type-check",
    "build": "pnpm -r --if-present build",
    "build:shared": "pnpm --filter @saas/shared build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prettier": "^3.4.2",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "pnpm": {
    "overrides": {
      "esbuild": ">=0.25.0"
    }
  }
}
